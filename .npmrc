# @keystatic/astro's published peer dep range is Astro 2-5, but Keystatic 5.x
# works fine at runtime with Astro 6 (the repo is built and tested on Astro 6).
# Remove this file once Keystatic publishes an updated peer range.
legacy-peer-deps=true
