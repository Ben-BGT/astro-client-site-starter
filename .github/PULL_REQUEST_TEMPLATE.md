## What changed

A short summary of the change.

## Why

Why this belongs in the template. Remember the starter is generic, so changes that only help one specific client site should live in a fork.

## Tested

- [ ] `npm run build` passes
- [ ] `npm run dev` loads the homepage
- [ ] `/keystatic` loads in the browser

## Notes for the maintainer

Anything else worth flagging.
